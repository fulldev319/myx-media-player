import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  groups: {
    marginTop: 10,
    paddingBottom: 50,
  },
  groupContents: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  loader: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
